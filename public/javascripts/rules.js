// Generated by CoffeeScript 1.7.1
var alterCredibility, downsize, income, stockOptions;

alterCredibility = function(playerIndex, amount) {
  io.connect(window.location.origin).emit("game:alterCredibility", {
    playerIndex: playerIndex,
    amount: amount
  });
  return true;
};

income = function(playerIndex) {
  alterCredibility(playerIndex, 1);
  return true;
};

stockOptions = function() {
  alterCredibility(playerIndex, 2);
  return true;
};

downsize = function() {
  alterCredibility(playerIndex, -7);
  return true;
};
