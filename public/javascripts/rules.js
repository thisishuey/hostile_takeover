// Generated by CoffeeScript 1.7.1
var downsize, income, increaseCredibility, stockOptions;

increaseCredibility = function(playerIndex, amount) {
  io.connect(window.location.origin).emit("game:increaseCredibility", {
    playerIndex: playerIndex,
    amount: amount
  });
  return true;
};

income = function(playerIndex) {
  increaseCredibility(playerIndex, 1);
  return true;
};

stockOptions = function() {};

downsize = function() {};
